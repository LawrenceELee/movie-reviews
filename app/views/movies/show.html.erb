<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @movie.title %>
</p>

<p>
  <strong>Year:</strong>
  <%= @movie.year %>
</p>

<p>
  <strong>In theater:</strong>
  <%= @movie.in_theater %>
</p>

<p>
  <strong>Is fav:</strong>
  <%= @movie.is_fav %>
</p>

<div id="reviews">
  <h1>Reviews</h1>
  <!-- The `reviews` method returns an array-like collection, so we can call `each` on it: -->
  <%= render @movie.reviews %>
</div>


<%= link_to 'Edit', edit_movie_path(@movie) %> |
<%= link_to 'Back', movies_path %>
